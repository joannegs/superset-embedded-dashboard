## Estrutura

```
/frontend   → React + Vite + TypeScript
/backend    → Node.js + Express + TypeScript
/superset   → configuração do Superset
docker-compose.yml → sobe Superset + Postgres + backend
```


### Todo 
#### Backend 
- Cachear o access_token de admin. Adicionar tratamento para validação de token e expiração