import { Router } from 'express'

const router = Router()

// TODO (Fase 2): POST / -> autentica no Superset (supersetAuth.service.ts) e
// devolve um guest token escopado ao dashboard configurado em SUPERSET_DASHBOARD_ID.

export default router
