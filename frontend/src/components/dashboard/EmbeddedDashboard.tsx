// TODO (Fase 2): usar @superset-ui/embedded-sdk (embedDashboard) para
// renderizar o dashboard, consumindo o token de guestTokenService.
// TODO (Fase 4): estados de loading e erro.

export function EmbeddedDashboard() {
  return null
}
